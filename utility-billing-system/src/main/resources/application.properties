spring.application.name=utility-billing-system
server.port=${SERVER_PORT:8080}

spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/utility_billing_db}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:wizard}
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.open-in-view=false
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.jdbc.time_zone=UTC

spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration

spring.mail.host=${SPRING_MAIL_HOST:smtp.gmail.com}
spring.mail.port=${SPRING_MAIL_PORT:587}
spring.mail.username=${SPRING_MAIL_USERNAME:Kelly.nendy@gmail.com}
spring.mail.password=${SPRING_MAIL_PASSWORD:tofyhzelozkhjsvn}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

app.jwt.secret=${APP_JWT_SECRET:change-this-secret-to-a-long-random-production-value-64-bytes}
app.jwt.access-token-minutes=${APP_JWT_ACCESS_TOKEN_MINUTES:480}
app.jwt.finance-access-token-minutes=${APP_JWT_FINANCE_ACCESS_TOKEN_MINUTES:1440}
app.jwt.refresh-token-days=${APP_JWT_REFRESH_TOKEN_DAYS:7}
app.otp.expiry-minutes=${APP_OTP_EXPIRY_MINUTES:15}
app.security.public-base-url=${APP_PUBLIC_BASE_URL:http://localhost:8080}
app.mail.from=${APP_MAIL_FROM:WASAC <Kelly.nendy@gmail.com>}
app.files.upload-dir=${APP_FILES_UPLOAD_DIR:uploads}
app.files.max-size-bytes=${APP_FILES_MAX_SIZE_BYTES:5242880}
app.bootstrap.admin-email=${APP_BOOTSTRAP_ADMIN_EMAIL:belyse457@gmail.com}
app.bootstrap.admin-phone=${APP_BOOTSTRAP_ADMIN_PHONE:+250788000000}
app.bootstrap.admin-password=${APP_BOOTSTRAP_ADMIN_PASSWORD:Admin@12345}

spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=5MB

springdoc.swagger-ui.path=/swagger-ui.html
springdoc.api-docs.path=/v3/api-docs
